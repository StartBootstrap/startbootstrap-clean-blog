name: Daily Rebuild

on:

  schedule:

    - cron: "0 0 * * *"

jobs:

  build:

    runs-on: ubuntu-latest

    steps:

    - uses: actions/checkout@v2

    - name: Build Jekyll Site

      uses: helaili/jekyll-action@v2.1.3

      with:

        args: --future --drafts --quiet

